services:
  - type: web
    name: jan-suvidha-ai
    env: node
    buildCommand: npm install && cd backend && npm install && cd ../frontend && npm install && npm run build && cd ../backend && npm run seed
    startCommand: cd backend && npm start
    envVars:
      - key: PORT
        value: 5000
